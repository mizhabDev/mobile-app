/**
 * PTP object types for the receive feature.
 * Re-exported from PtpService so screens/components don't reach into services directly.
 */
export type {
  PtpObjectMetadata,
  PtpImageData,
  PtpObjectHandle,
  PtpStorageId,
} from '../services/PtpService';
